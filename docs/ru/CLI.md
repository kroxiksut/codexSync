# Командная строка

[English](../en/CLI.md) · **Русский** · [中文](../zh/CLI.md)

[← Документация](README.md)

```powershell
codexsync -c config.toml <команда>            # после установки
python -m codexsync -c config.toml <команда>  # из исходников, без установки
codexsync-gui.exe -c config.toml <команда>    # оконная сборка для Windows, без консоли
```

Общие параметры: `-c/--config` — путь к `config.toml`; `-v/--verbose` —
подробный журнал, в том числе какие процессы Codex были замечены;
`-V/--version` — напечатать версию и выйти: так можно спросить скачанный exe,
что он такое. `-h` у любой команды и подкоманды выводит её параметры.

## Все команды

«Холодная» — команда отказывается работать, пока Codex не закрыт. Остальные
только читают (или пишут вне `.codex`) и работают в любой момент.

| Команда | Холодная? | Что делает | Подробно |
|---|---|---|---|
| `init-config` | — | Создать `config.toml` из встроенного шаблона | [ниже](#начало-работы) |
| `validate` | нет | Загрузить и проверить конфигурацию, больше ничего | [ниже](#начало-работы) |
| `config check` | нет | Показать, что эта версия изменила бы в `config.toml` | [Конфигурация](CONFIGURATION.md#обновление-конфигурации-от-предыдущей-версии) |
| `config upgrade` | нет | Применить это одной подтверждённой записью | [Конфигурация](CONFIGURATION.md#обновление-конфигурации-от-предыдущей-версии) |
| `doctor` / `preflight` | нет | Диагностика окружения; команды идентичны и без побочных эффектов | [ниже](#начало-работы) |
| `plan` | нет | Показать, что скопировала бы синхронизация (`volatile`, если Codex открыт) | [Синхронизация](SYNC.md) |
| `sync` | **да** | Синхронизация в обе стороны, сначала резервная копия | [Синхронизация](SYNC.md) |
| `restore` | **да** | Восстановить файлы из проверенной резервной копии | [Восстановление](RECOVERY.md#восстановление-из-резервной-копии) |
| `guardian watch` | нет | Непрерывно снимать глобальное состояние, пока Codex работает | [Хранитель](GUARDIAN.md) |
| `guardian snapshot --once` | нет | Сделать один снимок сейчас | [Хранитель](GUARDIAN.md) |
| `guardian list` | нет | Снимки этой машины и карантин | [Хранитель](GUARDIAN.md) |
| `guardian restore` | **да** | Вернуть проверенный снимок (без `--confirm` — предпросмотр) | [Хранитель](GUARDIAN.md#восстановление-из-снимка) |
| `guardian accept` | нет | Принять текущее состояние новой базой после настоящей потери | [Хранитель](GUARDIAN.md#новая-база) |
| `guardian scheduler` | нет | Устарела: используйте `automation apply` | [Конфигурация](CONFIGURATION.md#автоматизация) |
| `automation status` / `run` | нет | Показать задачу расписания или выполнить её безопасное действие сейчас | [Конфигурация](CONFIGURATION.md#автоматизация) |
| `automation apply` / `remove` | нет | Привести задачу ОС в соответствие с `[scheduler]` или удалить её | [Конфигурация](CONFIGURATION.md#автоматизация) |
| `sessions scan` | нет | Классифицировать все ветки сессий с обеих сторон | [Сессии](SESSIONS.md) |
| `sessions resolve` | нет | Записать одно решение по расхождению | [Сессии](SESSIONS.md#расхождения) |
| `sessions apply` | **да** | Перенести ветки целиком по подтверждённому плану | [Сессии](SESSIONS.md#применение-плана) |
| `sessions index` | нет | Показать, что лежит в каждом `session_index.jsonl` | [Сессии](SESSIONS.md#индекс-сессий) |
| `chats list` / `chats tree` | нет | Найти чаты и увидеть, под каким проектом каждый | [Проекты](PROJECTS.md#чаты) |
| `chats move` | **да** | Перенести выбранные чаты под один проект | [Проекты](PROJECTS.md#перенос-чатов-в-проект) |
| `repair-projects scan` | нет | Построить неизменяемый план ремонта | [Проекты](PROJECTS.md#ремонт-после-переезда-на-другую-машину) |
| `repair-projects apply` | **да** | Применить ровно один план по его id | [Проекты](PROJECTS.md#ремонт-после-переезда-на-другую-машину) |
| `project-move scan` | нет | Захэшировать проект и спланировать копирование в новую папку | [Проекты](PROJECTS.md#перенос-файлов-проекта) |
| `project-move apply` | **да** | Скопировать, проверить и переключить Codex на новую папку | [Проекты](PROJECTS.md#перенос-файлов-проекта) |
| `history` | нет | Показать прошлые синхронизации (или все виды записи) по их журналам | [Синхронизация](SYNC.md#история) |
| `recover inspect` | нет | Прочитать журнал операции без побочных эффектов | [Восстановление](RECOVERY.md#прерванные-операции) |
| `recover resume` / `rollback` | **да** | Закрыть прерванную операцию | [Восстановление](RECOVERY.md#прерванные-операции) |

Два правила действуют для каждой пишущей команды и не настраиваются: она
отказывает, пока Codex открыт *или его состояние не определено*, и делает
проверенную резервную копию до того, как что-либо заменить.

## Начало работы

Записать шаблон в `config.toml` в текущей папке, по другому пути или поверх
существующего файла:

```powershell
codexsync init-config
codexsync init-config --output D:\codexSync\config.toml
codexsync init-config --output D:\codexSync\config.toml --force
```

Или сразу заполненный для этой машины. Он проверяется, все комментарии шаблона
сохраняются, `--machine-id`, `--local-state-dir` и `--workspace-root` задаются
вместе (`--cloud-root` — по желанию), а существующий файл в этом режиме не
перезаписывается никогда:

```powershell
codexsync init-config --output config.toml --machine-id laptop-1 --local-state-dir C:/Users/me/.codex --workspace-root D:/Cloud/codexSync
```

### Какая конфигурация открывается

`-c` называет файл, и указать несуществующий путь — это просьба создать его там.
Конфигурация может лежать в любой папке. Без `-c` поиск такой же, как в окне:
конфигурация, которую окно открывало последней, затем `config.toml` в текущей
папке, затем рядом с исполняемым файлом. Если файла нет нигде, команда
завершается с кодом 4 и объясняет, как его указать, — место за вас никогда не
придумывается. О файле, найденном не в текущей папке, сообщается, чтобы команда
никогда не работала молча с файлом, на который вы не смотрите.

Окно записывает путь к открытой или созданной конфигурации в маленький
файл-указатель — только путь, не содержимое:
`%LOCALAPPDATA%\CodexSync\config-path.txt` в Windows,
`~/Library/Application Support/CodexSync/config-path.txt` в macOS и
`$XDG_CONFIG_HOME/codexsync/config-path.txt` в Linux. Так команда без `-c`
находит тот же файл, что и окно. В скрипте или задаче расписания `-c` всё равно
лучше указывать явно.

Проверить конфигурацию, затем окружение:

```powershell
codexsync -c config.toml validate
codexsync -c config.toml doctor
```

`doctor` (и его двойник `preflight`) только читает и ничего не создаёт — тем
более внутри `.codex`. Он проверяет конфигурацию и каталоги, запущен ли Codex,
схему глобального состояния и последний восстановимый снимок, файлы сессий и
индекс сессий, каталог потоков в SQLite, что разрешено синхронизации, манифест
синхронизации и оставшиеся временные файлы.

## Коды выхода

| Код | Значение |
|---|---|
| `0` | успех |
| `1` | ошибка выполнения |
| `2` | обнаружен конфликт, нужно решение |
| `3` | Codex запущен (не выполнено условие холодной операции) |
| `4` | неверная конфигурация или аргументы |
| `5` | безопасная остановка (fail-safe) |

`doctor`/`preflight` возвращают `0`, если все проверки прошли или есть только
предупреждения, и `5`, если хотя бы одна проверка провалена.

## Безопасность процесса

- codexSync никогда не запускает и не завершает Codex. Старые флаги завершения
  отклоняются с кодом `4`, как и `allow_terminate_if_running = true` для пишущих
  команд.
- Запись требует, чтобы Codex был непрерывно остановлен две секунды, и ещё
  прямых проверок до и во время фиксации.
- И `RUNNING`, и `UNKNOWN` блокируют запись. На macOS и Linux запись закрыта,
  пока определение процесса не подтверждено на живой машине (см.
  [что пока не подтверждено](README.md#что-пока-не-подтверждено)).
- Если файл назначения на мгновение занят другим процессом (облачный клиент,
  индексатор поиска, антивирус), атомарная замена повторяется с ограниченной
  паузой. Проверка процесса повторяется перед каждой попыткой, а другие ошибки не
  повторяются ([D-011](../dev/DECISIONS.md)).
- Фоновые процессы, означающие «Codex всё ещё работает», перечислены для каждой
  ОС в
  [`process_detection.background_process_names`](CONFIGURATION.md#process_detection).
- С `-v` команды `plan`, `sync` и `restore` пишут в журнал отслеживаемые
  процессы: запущены ли Codex и его песочница, и подпроцессы Codex (PID, имя,
  родительский PID). Полные командные строки не собираются никогда.
